paths:
  root:
    get:
      tags:
        - model
      summary: Models
      description: 'Dummy endpoint to trigger model generation'
      operationId: ignoreMe
      produces:
        - application/json
      responses:
        '200':
          description: results
          schema:
            "$ref": "#/definitions/ModelTrigger"
definitions:
  ModelTrigger:
    allOf:
      - $ref: '../../common/models.yaml#/definitions/TriggerObj2'
      - $ref: '../../common/models.yaml#/definitions/TriggerObj2'