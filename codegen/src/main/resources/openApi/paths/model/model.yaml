paths:
  root:
    get:
      tags:
        - model
      summary: Models
      description: 'Dummy endpoint to trigger model generation'
      operationId: ignoreMe
      produces:
        - application/json
      responses:
        '200':
          description: results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelTrigger'
components:
  schemas:
    ModelTrigger:
      allOf:
        - $ref: '../../common/models.yaml#/components/schemas/TriggerObj2'
        - $ref: '../../common/models.yaml#/components/schemas/TriggerObj2'